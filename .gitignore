# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated from content/comics/ by scripts/build-content.mjs (runs on every
# dev/build, and in CI before deploy) — not committed, so contributors only
# ever add a folder + image (and optionally a meta.json).
/public/comics/
/src/data/comics.json
content/comics/**/thumb.png
