# Public base URL of the site. Used to build absolute permalink / embed image
# URLs shown under each comic. The GitHub Actions deploy sets this for prod;
# leave the default for local dev.
NEXT_PUBLIC_SITE_URL="https://bambucd.xyz"
